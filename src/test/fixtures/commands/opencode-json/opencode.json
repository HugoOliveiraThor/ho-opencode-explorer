{
  "command": {
    "gsd-new-project": {
      "description": "Start a project from scratch",
      "template": "Run the /gsd-new-project command from GSD."
    },
    "gsd-map-codebase": {
      "description": "Map an existing codebase",
      "template": "Run the /gsd-map-codebase command from GSD."
    }
  }
}
