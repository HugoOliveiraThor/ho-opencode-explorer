# Supply Chain Risk Report

---

## Metadata

- **Scan Date**: 2026-08-11 12:10:56
- **Project**: ho-opencode-explorer
- **Repositories Scanned**: 7 (direct runtime deps: semver, yaml)
- **Total Dependencies**: 2 runtime / 18 devDependencies
- **Scan Duration**: ~10 min

---

## Executive Summary

The runtime surface is minimal and healthy: only `semver` and `yaml` ship in the extension (both org/company-backed, widely adopted, actively maintained, no open CVEs). All 7 npm-audit findings (and the 11 Dependabot alerts, which count the same advisories across the dep tree) are in **devDependencies only** — build/test/lint tooling that never ships in the `.vsix`. None affect end users of the extension.

### Counts by Risk Factor

| Risk Factor | Dependencies | Total |
|-------------|--------------|-------|
| Unmaintained | none | 0 |
| Low popularity | none | 0 |
| High-risk features | none | 0 |
| Presence of past CVEs | mocha (via serialize-javascript), @vscode/test-cli (via mocha), esbuild, commitlint (via fast-uri, js-yaml), eslint (via js-yaml, brace-expansion) | 5 |
| Single maintainer | standard-version (single maintainer, low activity) | 1 |
| Absence of security contact | — | 0 |
| **Total** | — | **6** |

### High-Risk Dependencies

The following dependencies have two or more risk factors.

| Dependency Name | Risk Factors | Notes | Suggested Alternative |
|-----------------|--------------|-------|-----------------------|
| (none meet the two-factor threshold) | — | — | — |

## Suggested Alternatives

No dependency requires replacement. The vulnerable packages are all dev-only tooling:

- **mocha 10.8.2** → depends on `serialize-javascript ^6.0.2` (no fix published for that range; 7.1.0 fixes RCE but is a major bump mocha doesn't accept). Dev-only; tests never parse untrusted input.
- **@vscode/test-cli 0.0.10** → depends on vulnerable mocha; official Microsoft tool, no alternative.
- **esbuild 0.24.2** → dev-server advisory (moderate); fix requires breaking 0.28.2. Build tool only; no dev server used.
- **commitlint 19.x** → transitive `fast-uri`/`js-yaml` advisories, both already fixed by `npm audit fix` (now 3.1.5 / 4.3.1).

## Report Generated By

Supply Chain Risk Auditor Skill
Generated: 2026-08-11 12:10:56
